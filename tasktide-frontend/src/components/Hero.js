import React from 'react';

const Hero = () => {
    return (
        <div className="hero">
            <h1>Welcome to TaskTide</h1>
            <p>Your local marketplace for services</p>
            <button className="cta-button">Get Started</button>
        </div>
    );
}

export default Hero;
